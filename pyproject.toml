[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "talespire-encoding"
version = "1.1.0"
description = "Encoding tools for TaleSpire data"
readme = "README.md"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["ts_encoding"]